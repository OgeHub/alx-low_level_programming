#include <stdio.h>

/**
 * main - Entry point
 * prints lowercase alphabets
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alphabet;

	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
	{
		putchar(alphabet);
	}
	putchar("\n");
	Return 0;
}
