#include <stdio.h>
#include "main.h"

/**
 * add - adds two numbers
 * @a: an integer
 * @b: an integer
 * Function that adds two integers
 * Return: the sum of a and b
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
