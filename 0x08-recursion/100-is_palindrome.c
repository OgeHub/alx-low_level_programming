#include <stdio.h>
#include "main.h"

/**
 * is_palindrome - function name
 * @s: input
 * Is  palindrome
 * Return: interger
 */
int is_palindrome(char *s)
{
	if (*s == '\0')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
